## Summary

<!-- What does this PR do, and why? -->

Fixes #<!-- issue number -->

## Checklist

- [ ] Targets `develop` (not `main`)
- [ ] `npm test` passes (`node --test test/*.test.mjs`)
- [ ] `npm run drift` is clean
- [ ] Scope traces to the linked issue — no unrelated drive-by changes
- [ ] If personas were added/changed, identity is behavioral
      (`rewards`/`punishes`/`quitsWhen`), not demographic
